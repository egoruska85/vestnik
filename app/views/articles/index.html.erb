<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%= render 'navbar' %>

    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-0 col-lg-2 fixed2">
  fsfdsfs
        </div>
        <div class="col-sm-12 col-lg-8">
          <div class="shadow-lg p-3 mb-5 bg-body-tertiary rounded">
            <center><h3><b>НОВОСТИ</b></h3></center>
            <hr>
            <h6>Все новости</h6>
            <hr>
            <% @articles.each do |article| %>
              <h3><%= article.title %></h3>
              <div class="row">
                <div class="col-8">
                  <%= link_to article_path(article), style:"text-decoration: none; font-size: 15px;" do %>
                    Подробнее
                  <% end %>
                </div>
                <div class="col-lg-12">
                  Опубликовал: <%= article.user.username %>; Дата: <%= article.created_at.strftime('%d.%m.%Y') %>
                </div>
              </div>


              <br>
              <div class="row">
                <div class="col-4 col-lg-2">
                  <%= image_tag(article.picture, style:"width: 6rem;") %>
                </div>
                <div class="col-8 col-lg-10">
                  <%= simple_format article.body.truncate_words(30) %>
                </div>
              </div>
              <hr>
            <% end %>
            <hr>

          </div>
        </div>
        <div class="col-sm-0 col-lg-2 fixed2">
dsfdsfsd
        </div>
      </div>
    </div>
  </body>
</html>
